__pycache__/
*.pyc
data/
.pytest_cache/
.venv/
venv/

# Placeholder tone fixtures from tools/render_voice.py --backend tone.
# The real recordings are .mp3; these exist only to drive the player in a
# test and must never ship.
/voice/*.wav

# The single-file build with every photograph baked in (tools/build_guide.py
# --offline). Made on demand for a stick or an email, never committed: it is
# most of ten megabytes of base64 that git cannot compress between versions.
/OFPE-Guide-offline.html
